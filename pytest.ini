[pytest]
testpaths = backend/tests
addopts = -ra --strict-markers --basetemp=backend/tests/tmp_pytest
markers =
    unit: Fast, isolated tests for pure logic and helpers.
    integration: Cross-module tests (API with fixtures, SQLite round-trips).
    e2e: End-to-end user-flow tests through API endpoints.
